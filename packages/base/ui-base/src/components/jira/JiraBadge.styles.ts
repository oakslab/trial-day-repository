import styled from 'styled-components';

export const Wrapper = styled.div`
  position: fixed;
  z-index: 100;
  right: 4px;
  bottom: 4px;
  cursor: pointer;
`;

export const Img = styled.img`
  width: 16px;
  height: 16px;
`;
